//set the order number on the order so that if we are chargin a room we can send the order number too
orderData.order_num = <%=@order.order_num%>;

if(isTableZeroOrder) {
  lastTableZeroOrder.order_num = orderData.order_num;
}