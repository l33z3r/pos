<div id="new_display">
  <section id="form_container">
    <h3>New Display</h3>

    <%= form_for([:admin, @disp ]) do |f| %>
      <% if @disp.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@display.errors.count, "error") %> prohibited this display from being saved:</h2>

          <ul>
            <% @disp.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div id="fields">
        <div class="field">
          <%= f.label :name %><br />
          <%= f.text_field :name %>
        </div>
        <div id="actions">
          <%= f.submit %>
          <%= link_to "Back", :back %>
        </div>
      </div>
    <% end %>

  </section>
</div>
