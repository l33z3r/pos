<% if !@error %>
  terminalID = "<%=@outlet_terminal_name%>";
  $('#terminal_id').html(terminalID);
  niceAlert("This terminal is now linked as <%=@outlet_terminal_name%>!");
<% else %>
  niceAlert("Error, terminal not found or already assigned!");
<% end %>