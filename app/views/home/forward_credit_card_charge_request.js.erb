<% if @error %>

  var errorMessage = "<%= @reason %>";
  creditCardChargeResponseCode = 7;
  creditCardChargeCallback(creditCardChargeResponseCode, errorMessage);

<% else %>

  <%= raw @forward_response.body %>

  //deal with the response creditCardChargeResponseCode json
  creditCardChargeCallback(creditCardChargeResponseCode, null);

<% end %>